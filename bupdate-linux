#!/bin/bash

fpath="/usr/share/games/freeciv"
[ ! -d "$fpath" ] && echo "Freeciv data directory not present at $fpath. Auto-installer will not proceed." && exit 0

path="$fpath/willset"
echo "Placing rulesets at $path"
